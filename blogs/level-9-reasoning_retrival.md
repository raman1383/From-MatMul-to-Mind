research project where we train a looped transformer(runs each layer twice) alongside a vector database 
for retrieval tasks, a small & fast "cognitive core" with updatable, vast and cheap knowledge base

